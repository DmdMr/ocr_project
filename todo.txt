File uploading + Handling:
- upload multiple formats, but only one image in the image, so I don't need to be cutting the image


Multiple select:
- we can select multiple documents just by hovering the card, and select will be in the left up corner. multiple actions for this: delete, add tag...

Image uploading
- upload multiple file extensions, images, pdfs. automatically split images if it's pdf

Card view:
- image needs to be the full height for the better view

Things to do:
- translate interface into Russian (all of the text visible to the user, all buttons...)
- have a mobile friednly interface, have a completely different view of the cards, how we have the card preview, and every other function
- now we have soring docs only by date added, so we need to change that to the alphabetical order
- for tags display only one line, so it will not cluster the interface, and take up the whole screen
- filename, now it's based on the date we uploaded the file and filename, but we need to have an ability to change it to something usefull, so add an ability to rename it
- better recognized text fiew and editing field, right now it's barely readable






Hardest things to implement:
- Advanced text recognition, like rows, lines and so on
- Text locaiton based where it was on the document
- Canvas for the previous point there, 
- Training a model for the recognition, so it would not make any mistakes, and searching won't be a problem


